services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: project_goit
      POSTGRES_PASSWORD: project_goit
      POSTGRES_DB: project_goit
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: